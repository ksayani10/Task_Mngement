{
  "name": "fs-intern-workspace",
  "private": true,
  "scripts": {
    "dev": "pnpm -r --parallel dev",
    "dev:api": "pnpm --filter ./api dev",
    "dev:web": "pnpm --filter ./web dev",
    "migrate": "pnpm --filter ./api migrate",
    "seed": "pnpm --filter ./api seed",
    "test": "pnpm --filter ./api test"
  }
}
